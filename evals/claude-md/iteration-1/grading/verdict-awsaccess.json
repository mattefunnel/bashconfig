{
  "rule_id": "awsaccess",
  "full_rate": 5,
  "ablated_rate": 5,
  "classification": "CUT_REDUNDANT",
  "one_line": "Model hands back AWS commands even without the rule; sandbox already prevents direct execution.",
  "reasoning": "FULL=5/5, ABLATED=5/5. Ablated compliance >=4, so the model complies without the rule -> REDUNDANT (cut). The sandbox network block and read-only tooling already cause the model to hand back commands rather than run aws-cli itself."
}
