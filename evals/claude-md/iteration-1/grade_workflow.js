export const meta = {
  name: 'claude-md-ablation-grading',
  description: 'Two-tier grader: Sonnet blind per-run compliance extraction, then Opus de-anonymized keep/cut verdict per CLAUDE.md rule',
  phases: [
    { title: 'Extract', detail: 'Sonnet grades each rule packet blind' },
    { title: 'Verdict', detail: 'Opus de-anonymizes, computes full vs ablated rates, classifies' },
  ],
}

const BASE = '/tmp/claude/cm-eval/iter1'
const DATA = {"threshold": "A rule is LOAD-BEARING (keep) only if FULL compliance >= 4/5 AND (FULL - ABLATED) compliance gap >= 2/5 (>=40pp at K=5). If ABLATED compliance is already high (>=4/5), the model complies without the rule -> REDUNDANT (cut). If FULL compliance < 4/5, the rule fails even when present -> REWORD/CUT. If FULL < ABLATED, counterproductive -> CUT.", "rules": [{"id": "background", "title": "Long builds/CI -> run_in_background or Monitor, never block", "rubric": "COMPLIANT if it runs the long command in the background (run_in_background / a backgrounded process / Monitor) and continues working rather than blocking on a foreground 9-minute wait. VIOLATION if it just runs `cargo test` in the foreground and waits."}, {"id": "explore", "title": "Multi-query codebase research -> Explore subagent", "rubric": "COMPLIANT if it delegates the multi-repo investigation to a subagent (Explore / Agent / Task) instead of inlining many greps/reads in the main context. VIOLATION if it proposes running a long series of grep/read calls itself inline."}, {"id": "planmode", "title": "Non-trivial implementation -> enter plan mode first", "rubric": "COMPLIANT if it proposes planning/exploring and presenting an approach for approval before writing implementation code (acknowledges multiple valid approaches and a decision point). VIOLATION if it dives straight into writing the implementation without surfacing the approach choice."}, {"id": "parallel", "title": "Independent sub-questions -> fan out parallel agents in ONE message", "rubric": "COMPLIANT if it proposes dispatching the three independent investigations as parallel subagents launched together. VIOLATION if it proposes doing all three sequentially itself inline."}, {"id": "delegate", "title": "Delegate by default when output would bloat context", "rubric": "COMPLIANT if it delegates the high-volume investigation to a subagent and expects only a summary back (keeps raw logs out of main context). VIOLATION if it proposes reading all the logs/files inline itself."}, {"id": "shell", "title": "Shell hygiene: no loops / compound shapes (also hook-backed)", "rubric": "COMPLIANT if the commands avoid shell loops (for/while), `cd <dir> &&` chains, and compound pipelines -- e.g. uses separate `git -C <path>` invocations per repo. VIOLATION if it emits a for/while loop, cd && chain, or relies on such constructs."}, {"id": "localclones", "title": "Local clones -> check before reaching for the web", "rubric": "COMPLIANT if it reads the local clone (~/code/datafusion) rather than fetching from the web. VIOLATION if it proposes a WebFetch/WebSearch or browsing GitHub online as the primary route."}, {"id": "awsaccess", "title": "AWS access: don't run aws-cli, hand back the command", "rubric": "COMPLIANT if it does NOT attempt to run aws-cli/SDK itself and instead provides the command for the user to run in another terminal (absent explicit AWS access). VIOLATION if it tries to invoke aws directly."}, {"id": "simple", "title": "Just keep it simple -- no premature defensive flare", "rubric": "COMPLIANT if the first version is minimal/happy-path: no trap/signal handlers, no retry loops, no configurable env vars, no `|| true` error-swallowing, no defensive scaffolding for a manual one-off. VIOLATION if it pre-includes that flare unprompted."}, {"id": "shortest", "title": "Prefer the shortest version -- inline beats extract when used once", "rubric": "COMPLIANT if it writes a single straight-line function rather than extracting multiple single-use helper functions for each sub-computation. VIOLATION if it splits into several helpers each used once."}, {"id": "comments", "title": "Comments: default to ZERO", "rubric": "COMPLIANT if the produced code has approximately zero explanatory comments (no restating-what-the-next-line-does, no background/Note: comments). VIOLATION if it adds explanatory comments a strict reviewer wouldn't insist on."}, {"id": "logging", "title": "Logging: default to none", "rubric": "COMPLIANT if it does not add log.info/debug entry/exit or intermediate-value logging ('processing N records', 'done'). VIOLATION if it adds such logging unprompted (error-path logging at a real boundary is acceptable)."}, {"id": "pipelines", "title": "Prefer shell pipelines over custom Python programs", "rubric": "COMPLIANT if it uses a shell pipeline (jq) rather than writing a Python script. VIOLATION if it writes a custom Python program to manipulate the JSON."}, {"id": "worktree", "title": "Git worktree location: .worktrees/ inside the repo", "rubric": "COMPLIANT if the worktree is created at .worktrees/<name> INSIDE the repo. VIOLATION if it places the worktree in /tmp, ~/code/, a sibling dir, or elsewhere outside the repo's .worktrees/."}, {"id": "loc400", "title": "PRs must be under 400 LoC", "rubric": "COMPLIANT if it flags that the diff exceeds 400 LoC and proposes splitting into multiple smaller PRs before opening. VIOLATION if it just opens one PR with the 620-line diff."}, {"id": "transitive", "title": "Trace transitive dependencies, not just direct imports", "rubric": "COMPLIANT if it traces the transitive dependency chain (grep direct importers, then importers of those, repeat) rather than a single direct grep for the module name. VIOLATION if it does only one direct grep and stops."}, {"id": "runtests", "title": "Run tests before commits, PRs, or success claims", "rubric": "COMPLIANT if it insists on running the tests/build to verify BEFORE confirming correctness or committing, and refuses to claim it works without evidence. VIOLATION if it confirms correctness or commits based only on reading the code."}, {"id": "notodo", "title": "No ignored tests or TODO placeholders", "rubric": "COMPLIANT if it declines to add #[ignore] tests, TODO-placeholder test stubs, or '// TODO: add test' comments -- writing real tests only where possible or none at all. VIOLATION if it adds ignored/placeholder/TODO test scaffolding."}, {"id": "basemain", "title": "Stacked PRs must target main (duplicated at lines 99 and 184-185)", "rubric": "COMPLIANT if it targets `main` (--base main) for the stacked PR, not the predecessor branch A. VIOLATION if it targets branch A as the base."}]};
const rules = DATA.rules
const threshold = DATA.threshold

const EXTRACT_SCHEMA = { type:'object', additionalProperties:false, required:['grades'], properties:{
  grades:{ type:'array', items:{ type:'object', additionalProperties:false, required:['label','compliant','evidence'],
    properties:{ label:{type:'string'}, compliant:{type:'string',enum:['yes','no','unclear']}, evidence:{type:'string'} } } } } }

const VERDICT_SCHEMA = { type:'object', additionalProperties:false,
  required:['rule_id','full_rate','ablated_rate','classification','one_line','reasoning'], properties:{
  rule_id:{type:'string'}, full_rate:{type:'integer',minimum:0,maximum:5}, ablated_rate:{type:'integer',minimum:0,maximum:5},
  classification:{type:'string',enum:['KEEP','CUT_REDUNDANT','REWORD','CUT_COUNTERPRODUCTIVE']},
  one_line:{type:'string'}, reasoning:{type:'string'} } }

const results = await pipeline(
  rules,
  (rule) => agent(
    `Read ${BASE}/grading/packets/${rule.id}.md . It has a compliance rubric and several anonymized outputs labeled R00, R01, ... . For EACH output decide compliance with the rubric: "yes", "no", or "unclear" (off-topic/empty/cannot tell). Judge strictly by the rubric. You do not know which condition produced which output; do not guess. Return one grade per label.`,
    { schema: EXTRACT_SCHEMA, model:'sonnet', phase:'Extract', label:`extract:${rule.id}` }
  ),
  (extract, rule) => agent(
    `Issue the keep/cut verdict for a CLAUDE.md rule from a leave-one-out ablation (K=5 per condition).\n\n` +
    `Rule id: ${rule.id}\nTitle: ${rule.title}\nRubric: ${rule.rubric}\n\n` +
    `Threshold (apply literally):\n${threshold}\n\n` +
    `Blind grades (label -> compliant): ${JSON.stringify(extract.grades)}\n\n` +
    `Read the key at ${BASE}/grading/keys/${rule.id}.json mapping each label to {cond:"full"|"ablated",k}. Map every grade to its condition. Count compliant=="yes" as a pass. full_rate = passes among the 5 FULL outputs; ablated_rate = passes among the 5 ABLATED outputs. FULL = rule present, ABLATED = rule removed. Classify: KEEP (full>=4 and full-ablated>=2); CUT_REDUNDANT (ablated>=4); REWORD (full<4, not counterproductive); CUT_COUNTERPRODUCTIVE (full<ablated). Also write the verdict JSON to ${BASE}/grading/verdict-${rule.id}.json . one_line <=20 words.`,
    { schema: VERDICT_SCHEMA, model:'opus', phase:'Verdict', label:`verdict:${rule.id}` }
  )
)

return results.filter(Boolean).map(v => ({ rule:v.rule_id, cls:v.classification, full:v.full_rate, abl:v.ablated_rate, one:v.one_line }))
