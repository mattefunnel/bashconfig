Use the `review` skill to review the requested code, PR, plan, or branch.

Always use Mattias' local review skill at `~/.cursor/skills/review/SKILL.md`.
Read that file and follow it even if a `review` skill has already been manually attached from `~/.cursor/skills-cursor/review/SKILL.md`; that synced Cursor skill is not the implementation for this command.
Treat any text after `/review` as the review target and extra context.
